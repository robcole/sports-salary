require 'spec_helper'

describe TeamBuilder do
  it 'has a version number' do
    expect(TeamBuilder::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
